use tokio::main;
use teo_result::Result;

#[main]
async fn main() -> Result<()> {
    // let app = App::new()?;
    // AppCtx::get()?.set_entrance(Entrance::CLI)?;
    // app.run().await
    Ok(())
}
